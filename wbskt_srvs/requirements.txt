# WebSocket / Coinbase
websockets==12.0

# Redis client (both for direct pub/sub and for Django's Redis cache)
redis==5.0.1
django-redis==5.4.0

# Django stack
Django==4.2.11
asgiref==3.8.1
django-environ==0.11.2

# DB driver (Postgres)
psycopg2-binary==2.9.9

# Env
python-dotenv==1.0.0

# Celery (only needed because cb_trades.tasks imports @shared_task and
# dispatches .delay(); the WebSocket service does not run a worker, but it
# must import the module without errors).
celery==5.6.3
boto3==1.43.93
django-celery-beat==2.9.0