# TODO



Analyze my websocket setup.
Optimize all code to prevent ram consumption growth over a long periods of running time.
Consider that i need strategy_s1 function and redis store price function to run for every product_id that is subscribed to
I will include file by file all code related do the websockets, cache storage and more.
Provide fully generated files with all related imports.
Provide explanation.



After Deployment and testing:
    Aws store files with websocket messages INSTEAD of DB storage



When switching do AMD from Amper
    postgres_data should be a docker volume
    Script to export all WorkWeek and ParkJob models. Preserve relations



- Sending the email:

    Idempotency: Ensure your email provider supports a unique Message-ID 
        or use a dedup key in your email headers so even if a fluke happens, 
        the email client/SMTP server sees it as a duplicate.
    Pro-Tip: Since your task queue is dedicated, 
    set the worker concurrency for that specific queue to --concurrency=1. 
    This forces sequential processing. Combined with the Redis lock, 
    this makes a double-send mathematically impossible.


# All of this after Bitrow.com !!!!!
    - alert for a break out price for custom amount of days

    - RETEST TRADE OPENING 
        by having an array of prices per currency with each price increased, 
            being the new 20day breakout.
            with every message from websocket replace price 
            with gradual increase see how application reacts.
    
    - Css gauges that takes in 10_day, 20_day and 55_day highs and lows
        and arranges both on each side of the gauge starting with the highest to lowest.
    - Css Gauge must be animated and reflect price changes per ticker_symbol.

    
Nice to have
    - a line with break out events and time period between them for every currency
        currntly subscribed with web sockets.








    - ✅ celery task to setup last trades (initial trades) according to trading pairs provided.
    - ✅ front end Closed Trades beter decimal formatting.
    - ✅ change address in a 10 day email to mkla.rbzk@gamil.com


    - ✅ Websocket s1_strategy no longer a task. Spawn a task only on the exect breakout.
        WebSocket Layer: Drops 99.9% of messages. 
        Spawns a Celery task only on the exact upward break-out.

    - ✅ Celery Task (single queue, concurrency=1 or 2):
        First, attempts a Redis SET NX EX (fastest, handles milliseconds).

        If Redis succeeds, it performs a final atomic UPDATE in the DB 
        (for guaranteed persistence) then sends an email


    - ✅ fontend ajax to backend and backend to resubscribe with websockets.
    - ✅ front ent tables to display real time prices, highs, lows.
    - ✅ front end tables to display current trades and prices.
    - ✅ Connect S1 strategy.
    - ✅ retest the S1 / highs_and_lows
    - ✅ retest the emails
    - ✅ 10 day break email


!! ✅ Cache update last_trade to last_trades object, that stores last trade per product_id

#  ✅ Celery have 3 Queus by priority.
#       1-high = websocket messages
#       2-default = trading strategies, db operations


#✅ include compression header for websocket connection
#✅ autorestart the websocket on error
#✅ Error 104 :Connection closed by peer Solution
#✅ wsClient = cbpro.WebsocketClient(url="wss://ws-feed.pro.coinbase.com",
#✅                                 products=["BTC-USD", "ETH-USD"],
#✅                                 channels=["ticker"])
#✅ wsClient.start()
#✅ while true:
#✅         if wsClient.error:
#✅                 wsClient.close()
#✅                 wsClient.error = None
#✅                 wsClient.start()
#✅         time.sleep(1)

# ✅ clean up history data from coinbase. rearange the code.
# ✅ historical data 55 days from coin base.
# ✅ clean up migrations

# ✅setup email for webvision ltd
# ✅ S1 Strategie with a model
# ✅ S2 strategie with a model
# ✅ BUG :After a long websocket job with a lot of messages only 1 gets added to db
# ✅one websocket connection never multiple
# ✅Mark Trading pairs  outside of celery task
# ✅struckture websocket messages
# ✅write ws messages in to redis
# ✅every minute empty redis cache
# ✅two redis entries to hold messages one by one.
# ✅when the celery tasks runs too empty one redis array
# ✅the other one is being prepopulated
# ✅flip the flag
# ✅empty redis array is being prepopulted
# ✅run the query against the full one.

